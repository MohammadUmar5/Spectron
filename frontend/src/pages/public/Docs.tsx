import { LandingWrapper } from "../../hoc";

const Docs = () => {
  return (
    <div className="flex items-center justify-center mt-24">
      <p className="text-3xl w-1/3 text-center font-bold text-accentColor">
        Documentation is not available yet but will soon be added
      </p>
    </div>
  );
};

export default LandingWrapper(Docs);
