import { FeaturesSection } from "../../components";
import { LandingWrapper } from "../../hoc";

const Features = () => {
  return (
    <>
      <FeaturesSection />
    </>
  );
};

export default LandingWrapper(Features);
